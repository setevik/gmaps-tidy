// gmaps-tidy options page
// Category mapping editor, audit settings, move log viewer.

const root = document.getElementById("options");
if (root) {
  root.textContent = "gmaps-tidy options — implementation in progress.";
}
